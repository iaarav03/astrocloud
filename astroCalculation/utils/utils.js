exports.formatHoroscopeRequest = (data) => {
  // For example, you could convert the date string (YYYY-MM-DD) into a Date object.
  // For simplicity, we return the data as is.
  // You may add more complex formatting or validation logic here.
  return data;
};
